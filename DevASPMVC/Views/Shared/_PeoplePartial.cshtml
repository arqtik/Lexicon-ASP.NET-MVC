@model IEnumerable<Person>

<div class="row primary-bg">
    <div class="col-2 col-border col-padding">First Name</div>
    <div class="col-2 col-border col-padding">Last Name</div>
    <div class="col-1 col-border col-padding">Gender</div>
    <div class="col col-border col-padding">City</div>
    <div class="col col-border col-padding">E-mail</div>
    <div class="col-1 col-border col-padding">ID</div>
</div>
@{
    foreach (var person in Model)
    {
        <partial name="_PersonPartial" model="@person"/>
    }
}